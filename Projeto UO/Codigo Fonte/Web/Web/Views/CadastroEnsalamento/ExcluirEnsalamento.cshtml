@model Web.ViewModel.EnsalamentoVM
@using (Html.BeginForm("Excluir", "CadastroEnsalamento", FormMethod.Post)) {
    @Html.AntiForgeryToken()
    <div class="box-body">
        <div class="box-header with-border">
            @Html.HiddenFor(model => model.Ensalamento.Id)
            <h3 class="box-title">Deseja realmente excluir o ensalamento do(a)<label>@Html.DisplayTextFor(model => model.Ensalamento.Ocupante)</label>?</h3>
        </div>
        <div class="box-footer">
            <div class="text-center">
                <button type="submit" class="btn bg-orange btn margin"><i class="fa fa-check"></i> Excluir</button>
                <button type="button" class="btn bg-navy btn margin" onclick="CancelarWindowsForms()"><i class="fa fa-close"></i> Cancelar</button>
            </div>
        </div>
    </div>
}
