@using (Html.BeginForm("SairSistema", "Login", FormMethod.Post)) {

<div class="box-body">
    <div class="box-header with-border btnss">
        <h3 class="box-title">Deseja realmente sair do sistema ?</h3>
    </div>
    <div class="box-footer">
        <div class="text-center">
            <button type="button" class="btn bg-navy btn margin" onclick="CancelarWindowsForms()"><i class="fa fa-close"></i> Cancelar</button>
            <button type="submit" class="btn bg-orange btn margin"><i class="fa fa-check"></i> Sair</button>
        </div>
    </div>
</div>
}
